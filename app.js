// Campus Companion JavaScript will be introduced in Phase 1.5.
//
// Until then, the responsibilities are intentionally separated:
// - index.html owns the page structure and content.
// - styles.css owns colors, layout, responsive rules, and visual states.
// - app.js will own events, state, and DOM updates in a later phase.
//
// Keeping this file as a visible entry point helps teammates know where
// interactive behavior should be added without mixing it into the HTML.
